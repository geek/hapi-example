{
  "name": "ProductsAPI",
  "version": "0.0.3",
  "main": "server",
  "engines": {
    "node": ">=4.0.0"
  },
  "dependencies": {
    "code": "^2.0.1",
    "handlebars": "1.0.x",
    "hapi": "11.x.x",
    "joi": "7.x.x",
    "lab": "^7.3.0",
    "lout": "7.2.x"
  },
  "private": "true",
  "scripts": {
    "test": "lab -a code -c",
    "test-tap": "lab -a code -r tap -o tests.tap",
    "test-cov-html": "lab -a code -r html -o coverage.html"
  }
}
